# Digistore24 API Key
# Hol dir den Key unter: Digistore24 → Settings → Account Access → API Keys
DIGISTORE_API_KEY=dein-api-key-hier

# Admin-Passwort für den Notfall-Login (Fallback wenn Supabase nicht erreichbar)
ADMIN_PASSWORD=dein-passwort-hier

# Basis-URL (für Produktion auf dem Raspberry Pi anpassen)
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Supabase (Auth + Storage + DB)
# Hol dir die Werte unter: supabase.com/dashboard → Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://dein-projekt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=dein-anon-key-hier
SUPABASE_SERVICE_ROLE_KEY=dein-service-role-key-hier

# Admin-E-Mails (komma-getrennt) – diese User können Fotos löschen
# Alle anderen @intumind.de User können nur hochladen
ADMIN_EMAILS=marc@intumind.de
NEXT_PUBLIC_ADMIN_EMAILS=marc@intumind.de
